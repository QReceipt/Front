import React from "react";
import Title from "../atoms/Title.js"

const Logo = ({title}) => {
    return (
        <div>
            <Title title = {title} />
        </div>
    )
}

export default Logo;